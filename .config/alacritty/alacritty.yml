#!/usr/bin/alacritty --config-file

import:
  - ~/.config/colors/alacritty/github-vscode-dark.yml

shell: /usr/bin/dvtm

env:
  TERM: alacritty+common

font:
  normal:
    family: JetBrains Mono NL

window:
  dynamic_padding: true
